<!DOCTYPE html>
<html lang="en" class="h-full bg-white">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Simple Car Rental{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="min-h-full text-slate-800">
        <header class="border-b border-slate-200 bg-white/70 backdrop-blur supports-[backdrop-filter]:bg-white/60">
            <nav class="mx-auto max-w-7xl px-6 py-4 flex items-center justify-between">
                <a href="{{ path('app_home') }}" class="font-extrabold tracking-tight text-xl">Simple Car Rental</a>
                <div class="flex items-center gap-3">
                    {% if app.user %}
                        <a href="{{ path('app_cars_search') }}" class="text-slate-700 hover:text-slate-900">Cars</a>
                        <a href="{{ path('app_reservations') }}" class="text-slate-700 hover:text-slate-900">Reservations</a>
                        <span class="hidden sm:inline text-slate-500">{{ app.user.userIdentifier }}</span>
                        <a href="{{ path('app_logout') }}" class="inline-flex items-center rounded-md bg-slate-900 text-white px-4 py-2 text-sm font-medium shadow hover:bg-slate-800 transition">Logout</a>
                    {% else %}
                        <a href="{{ path('app_home') }}" class="text-slate-700 hover:text-slate-900">Home</a>
                        <a href="{{ path('app_login') }}" class="text-slate-700 hover:text-slate-900">Login</a>
                        <a href="{{ path('app_register') }}" class="inline-flex items-center rounded-md bg-slate-900 text-white px-4 py-2 text-sm font-medium shadow hover:bg-slate-800 transition">Register</a>
                    {% endif %}
                </div>
            </nav>
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>

        <footer class="mt-12 py-8 text-center text-sm text-slate-500">
            © {{ "now"|date("Y") }} Simple Car Rental
        </footer>
    </body>
</html>
